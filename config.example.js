/**
 * Configuration de l'application - TEMPLATE
 *
 * Instructions :
 * 1. Copiez ce fichier et renommez-le en "config.js"
 * 2. Remplacez 'YOUR_GITHUB_TOKEN_HERE' par votre token GitHub
 * 3. Générer un token : https://github.com/settings/tokens
 *    - Sélectionnez "Generate new token (classic)"
 *    - Cochez le scope "gist"
 *
 * IMPORTANT : Ne jamais commiter config.js sur GitHub!
 */

window.APP_CONFIG = {
    // Votre Personal Access Token GitHub (scope: gist)
    githubToken: 'YOUR_GITHUB_TOKEN_HERE',

    // ID du Gist (laissez vide pour la première utilisation, il sera rempli automatiquement)
    gistId: null
};
